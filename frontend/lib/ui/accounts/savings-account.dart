import 'package:flutter/material.dart';

class SavingsAccountPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Savings Account'),
      ),
      body: const Center(
        child: Text('Savings Account Page'),
      ),
    );
  }
}